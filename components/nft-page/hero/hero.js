import React from 'react'
import classes from './hero.module.scss';

const Hero = () => {
  return (
    <section className={classes.hero}>
        <div className={classes.image}>hero</div>    
    </section>
  )
}

export default Hero