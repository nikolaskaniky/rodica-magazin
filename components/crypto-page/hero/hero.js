import React from 'react'
import classes from './hero.module.scss';

const Hero = () => {
  return (
    <section className={classes.hero}>
        <p>hero</p>
    </section>
  )
}

export default Hero